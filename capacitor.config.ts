import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.workoutactivitytracker',
  appName: 'Workout Activity Tracker',
  webDir: 'dist',
};

export default config;
